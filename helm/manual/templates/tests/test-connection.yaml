apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "manual.fullname" . }}-test-connection"
  labels:
    {{- include "manual.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "manual.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
